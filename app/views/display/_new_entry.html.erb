<article>
	<div>
		Ingresa el código de tu ticket de compra:
	</div>
	<%= form_for(@entry, :html=> {:id => "entry-form"}, :remote => true) do |f| %>
	  <% if @entry.errors.any? %>
	    <div id="error_explanation">
	      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

	      <ul>
	      <% @entry.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	      </ul>
	    </div>
	  <% end %>

	  <div class="field">
	    <%= f.hidden_field :user_uid, :value => current_user.uid %>
	  </div>
	  <div class="field">
	    <%= f.label :ticket_number %><br />
	    <%= f.text_field :ticket_number %>
	  </div>
	  <div class="actions">
	    <%= f.submit 'CONTINUAR', :id => 'ticket-btn'%>
	  </div>
	<% end %>
</article>